# [Smoketest] Rapid Cycle
**Date:** 2026-04-13 17:36:27
**Scenario:** rapid-cycle
**Duration:** 2m 20s (run 1 only — aborted after failure)
**Result:** FAIL

## Phases
| Phase | Status | Duration | Notes |
|-------|--------|----------|-------|
| run-1 pre-build | PASS | ~18s | Contract deployed, Extension ID 0x19c |
| run-1 start-services | PASS | ~74s | Docker build + containers started |
| run-1 post-build | FAIL | ~48s | TEE registration failed with 404 from normal proxy |
| run-1 test | SKIPPED | - | Not reached due to post-build failure |
| run-2 | SKIPPED | - | Not attempted due to run 1 failure |

## Errors
TEE registration failed during the availability check step:
```
WARN  fccutils/tee_calls.go:93  action result status not ok: got: 404 for 0xa97b4bd1..., https://tee-proxy-coston2-1.flare.rocks
FATAL fccutils/encoding.go:27  Error: action result status not ok, got: 404
```

The availability check was sent successfully (`instructionId: a97b4bd1...`), but when polling for the result via the normal proxy (`tee-proxy-coston2-1.flare.rocks`), a 404 was returned. The pre-registration and TEE attestation steps both succeeded. This suggests a transient issue with the normal proxy or a timing problem where the result wasn't available within the polling window.

Stack trace: `ActionResult` -> `GetFTDCAvailabilityCheckResult` -> `RegisterNode` -> `main`

## Observations
- This is the first TEE registration failure in this session (across ~20+ successful runs).
- The failure occurred on the normal proxy (`tee-proxy-coston2-1.flare.rocks`), not the extension proxy (ngrok). This is an external infrastructure dependency.
- The pre-registration and attestation steps succeeded — only the availability check result polling failed.
- The 404 response came after ~33s of waiting (14:455 to 47:904), suggesting the result simply never appeared rather than being a quick rejection.
- This is likely a transient infrastructure issue rather than a code bug, as the same flow has succeeded consistently in all previous runs.
